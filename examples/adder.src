function main()
{
    var n = 0;
    forever {
        say("Running total:");
        write(n);
        let m = ask("Number to add:");
        if m == 0 {
            return;
        }
        n = n + m;
    }
}
